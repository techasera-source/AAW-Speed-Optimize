# Filled in automatically by `shopify app dev` / `shopify app deploy`.
# For local manual runs, copy this to .env and fill in real values.

SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SCOPES=read_themes,write_themes
SHOPIFY_APP_URL=https://your-tunnel-url.trycloudflare.com
